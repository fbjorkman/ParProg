To compile matrixSum.c typ "gcc matrixSum.c -pthread -o matrixSum" and run with ./matrixSum
To compile quicksortST.c typ "gcc quicksortST.c -o qst" and run with ./qst
To compile quicksortMT.c typ "gcc quicksortMT.c -pthread -o qmt" and run with ./qmt

I didnt get the multithread to work, but I'll submit what I have written so far.